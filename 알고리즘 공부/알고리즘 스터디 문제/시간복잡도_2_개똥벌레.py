import sys

n,h = map(int, sys.stdin.readline().split())

arr = [[0 for i in range(n)] for j in range(h)]

for k in range(n):
  huddle = list(int(input))
  huddle[0]
  if k % 2 == 0:
    huddle[k] = arr[n][h]
  
print(arr)
    