// function solution(num_list) {
//   let arr = [0, 0];
//   for (let i = 0; i < num_list.length; i++) {
//     if (num_list[i] % 2 === 0) {
//       arr[0]++;
//     } else {
//       arr[1]++;
//     }
//   }
//   return arr;
// }

// for i in arr 는 arr의 인덱스 값을, for i of arr 는 arr의 객체값을 가져온다.
// // array의 값을 바로 ++로 증가 가능
// // filter 함수사용해서 가능
